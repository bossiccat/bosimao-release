# 交接审计报告 — 贾克斯模式（监视app）

> 审计人：项目总监（大湾区靓仔）| 审计日期：2026-08-03
> 审计范围：上轮会话全部产出（契约文档 / 后端 / 前端 / 脚本 / 配置 / PoC）
> 审计方法：契约对照 + 代码走查 + 静态扫描（P0 三规则）+ 实测验证（pytest / import / build / 运行时）

---

## 1. 审计总评

**上轮会话实际进度**：M0 契约化基本完成 + 约 60% V1 代码骨架。**核心违规：POC 未实测（三大风险全"待执行"）就写了 V1 代码**，违反计划 §9.1 M1 门禁（"B1+B2+B3 全过 → 全面开发 V1"）。

| 维度 | 评分 | 说明 |
|---|---|---|
| 契约文档（PRD/ARCH/DSGN/SPEC/ADR） | 8.5/10 | 体系完整、质量高（EARS/RICE/选项对比） |
| 后端代码 | 7.0/10 | 分层清晰、核心纯函数优秀；**3 个实锤 bug + 1 个悬空流程** |
| 前端代码 | 7.5/10 | Token/状态机/Lucide 优秀；**从未构建成功**（缺 tsconfig + icons） |
| 运维脚本 | 6.0/10 | 结构合理；**setup_env/dev 的 $Root 路径计算有 bug** |
| 测试 | 7.5/10 | 21/21 通过（纯逻辑）；**无硬件链路测试**（WGC/模型/推送未覆盖） |
| PoC 实证 | 2.0/10 | 三份报告全部"待执行"，**零实测数据** |
| 工程基建 | 4.0/10 | **无 git / 无 pyproject / 无 .env** |

**交接结论**：代码骨架可用但**不可交付**。必须先修 P0 缺陷 → 执行 PoC 实测 → 再按结果裁决 V1/V1.1 范围。

---

## 2. 实锤缺陷清单（按优先级）

### P0 — 阻断交付（必须修复）

| # | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| 1 | **WGC 授权流程悬空**：`SessionManager.mark_authorized()` 无任何调用方；`start_wgc()` 要求 `authorized=True`（初始 False） | session_manager.py:68-79 + 全局检索 | **WGC 捕获永不启动，监控实际不可用**（仅走 DXGI 兜底） |
| 2 | **config 加载层级 bug**：`load_detection()`/`load_push()` 将带包层（`detection:`/`push:`）的 yaml 整体 validate，字段落在子键 → 全部落入默认值 | 实测注入 5/180/3 → 输出 3/120/2 | **改 yaml 不生效**，违反计划 §6.1 核心承诺（防误判靠参数承诺失效） |
| 3 | **DXGI 兜底是整屏截图**（`display=0` 全屏），非"按窗口区域裁剪" | dxgi_fallback.py:31-42 + session_manager.py:95-100 | 窗口最小化时喂给视觉模型的是**全屏画面**，判定内容错误 |
| 4 | **前端无法构建**：缺 `tsconfig.json`（`tsc` 直接报错）；`tauri.conf.json` 引用 `icons/tray-icon.png` + `icon.ico` 但 icons/ 目录不存在 | `npm run build` 实测失败 + ls 确认 | Tauri 桌宠**无法打包/运行** |
| 5 | **视觉分析无降采样**：`CaptureConfig.max_width=1280` 已定义但 `vision_analyzer` 直接喂原图 | vision_analyzer.py:73-75 | 12G 显存 OOM 风险 + 单帧延迟超预算（B1 通过标准要求降采样后测） |

### P1 — 影响交付质量

| # | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| 6 | `WgcCapturer.stop()` 只置标志，`capture.start()` 阻塞线程不会退出 | wgc_capture.py:75-77 | 会话无法干净停止，重启泄漏线程 |
| 7 | WGC 帧文件无限累加不清理（`frame_{n}.png`） | wgc_capture.py:64 | 长期运行磁盘膨胀（计划 R12） |
| 8 | `routes_control.trigger_alert_test` 用 `type("R", ...)` 动态 hack + 访问私有 `_maybe_alert` | routes_control.py:44-50 | 脆弱、不可测试、违反分层 |
| 9 | SPEC §4.1 点名缺失：`engine/advice_generator.py` 不存在；`voice/` 目录**为空**（vad/duplex/stt/tts 全无） | ls 实测 | SPEC 违约；V1.1 零实现 |
| 10 | SPEC §4.2 点名缺失：`components/ReminderToast.tsx`、`components/Settings.tsx` 不存在（toast 内联在 App.tsx） | ls 实测 | SPEC 违约；设置能力缺失 |
| 11 | **docs/specs/ 空目录**——计划 A9 要求的 4 份组件契约（backend-monitor / detection-engine / pet-ui / push-provider）未产出 | ls 实测 | 组件级契约缺失，前后端对齐缺依据 |
| 12 | openapi.yaml 点名 `/api/v1/voice/wake|suspend`，后端未实现 | openapi.yaml + routes 检索 | 契约与实现不一致（V1.1 排期，需登记 OPEN-DECISIONS） |
| 13 | 无 `.git`（无回退）、无 `pyproject.toml`（ruff/mypy 未配置）、无 `.env`（推送不可用） | ls 实测 | 发布安全（计划 §6.4）违约 |
| 14 | `scripts/setup_env.ps1` + `dev.ps1` 的 `$Root` 计算多 split 一层（`Split-Path (Split-Path $PSScriptRoot -Parent) -Parent`） | 脚本审查 | .venv/依赖装到**上级目录**，脚本实际无法一键初始化 |

### P2 — 纪律项

| # | 缺陷 | 证据 |
|---|---|---|
| 15 | `scripts/poc_002_capture.py:5` 注释含 emoji（⚠️） | P0-1 扫描命中（注释非图标，按纪律清除） |
| 16 | SPEC §3 Lucide 版本记录与 package.json 不符（"1.16.0" vs ^0.469.0） | 契约与实现不一致 |
| 17 | `tauri.conf.json` `csp: null` | Tauri 官方建议配置 CSP（MVP 可登记后补） |
| 18 | `Settings.model_dir` 默认硬编码 `D:\models` | 应仅由 .env 提供（默认值可留但需 README 声明） |

---

## 3. 健康项（上轮做对的部分，保留）

- ✅ **契约体系完整**：PRD（EARS 句式 + RICE）/ ARCHITECTURE（进程拓扑 + 显存策略）/ DESIGN（Token 全表 + 六态机 + 四级打扰）/ SPEC（版本锚定 + 已知坑）/ openapi / ADR-001~008（含 3 方案对比）/ OPEN-DECISIONS（4 未决项）
- ✅ **status_detector 纯函数** + 变异定向加固测试（2/3/4 帧边界、119/120s 边界、恢复清除）——21/21 实测通过
- ✅ **后端分层规范**：api / core / capture / engine / push / services / utils，依赖只向下
- ✅ **前端 XState 六态机**完整（含 barge-in"压扁不重开"、reduced-motion 降级）
- ✅ **Design Token** 无硬编码色值；Lucide 图标落地（MonitorPanel 实际使用）
- ✅ **P0 三规则扫描通过**：无 emoji 图标 / 无紫粉渐变 / 无模板味文案；全部文件 ≤300 行
- ✅ **orchestrator**：LLM 时分复用锁 + 对话降频 + 提醒节流
- ✅ **PushManager**：Provider 路由 + 熔断 + 重试 + 限频（实现完整）
- ✅ **window_finder 实测正常**（当前环境 Hermes 窗口定位成功；上轮日志 win32gui 报错为旧版 pywin32 兼容问题，现行代码已用 win32process 正确写法）
- ✅ 三份 **POC 报告模板**质量高（步骤/通过标准/B 计划齐全，待执行）

---

## 4. PoC 状态与前置

| PoC | 状态 | 前置条件 | 关键通过标准 |
|---|---|---|---|
| B0 | ⏸ 未开始 | 模型下载 8.3GB（ModelScope）+ Comni 安装 | — |
| B1 模型视觉 | 待执行 | B0 | 显存 ≤10.5GB；单帧判定 ≤4s；JSON 三态稳定 |
| B2 WGC 捕获 | 待执行 | 三 App 窗口真实存在（**Hermes 当前运行中**） | 60 帧有效帧率 ≥95%；最小化恢复 ≤3s |
| B3 全双工语音 | 待执行 | B0 | 端到端 ≤1.5s；打断 <500ms；VAD 误触发 ≤5% |

**PoC 裁决规则**（计划 §8.1）：任一失败且备用不可行 → 按 B 计划降级，不硬扛。

---

## 5. 修复方案（建议分工，待总监编排确认）

| 修复项 | 负责专家 | 验收标准 |
|---|---|---|
| config 层级 bug（detection/push） | 架构师 + 后端 | 注入 5/180/3 → 加载 5/180/3；QA 补回归测试 |
| WGC 授权流程（授权端点 + 状态机 + 引导弹窗） | 架构师设计 + 后端实现 | `POST /api/v1/capture/authorize` 可触发；授权后 WGC 真启动 |
| DXGI 窗口区域裁剪 | 后端 | 窗口坐标 → 整屏裁剪 → 单窗口图；最小化场景可监控 |
| 视觉降采样 | 后端 | max_width 生效；截图预处理单元测试 |
| 前端构建修复（tsconfig + icons + ReminderToast + Settings） | 前端 | `npm run build` 通过；tauri 配置校验过 |
| specs/ 组件契约 4 份（A9 补齐） | 架构师 | 每份自包含（文件/接口/验收/e2e） |
| advice_generator.py | 后端 | SPEC 签名一致；单测覆盖 |
| 运维脚本 $Root bug + git init + pyproject.toml | 运维 | `setup_env.ps1` 实跑成功；`git log` 存在 |
| trigger_alert_test 重构（去 hack） | 后端 | 走公开 API；单测覆盖 |
| WGC stop/帧清理 | 后端 | stop 后线程退出；帧目录有上限 |

---

## 6. 交接裁决

1. **不能直接进入全面开发**——P0-1（WGC 悬空）与 P0-2（配置失效）使现有代码实际不可用；P0-4（前端不能构建）阻断桌宠交付。
2. **必须先补基线**：修复 P0 缺陷 + 补齐 A9 契约（预计 1 个工作日），同时后台启动 B0 模型下载（8.3GB 为最长耗时项）。
3. **PoC 门禁不撤**：B1/B2/B3 实测结果决定 V1/V1.1 范围与版本节奏（OPEN-DECISIONS O-001 由本项目总监裁决）。
4. **用户需配合项**：① 确认三文档 + SPEC（唯一交互点）；② 提供手机推送 webhook（O-002）；③ WGC 授权弹窗需人工点"允许"；④ 确认被监控 App 清单（Hermes 当前在线，Codex/Trae 未运行）。

---

## 7. 专家团七人独立审计汇总裁决（2026-08-03 01:25 追加）

> 7 位专家（PM/架构师/设计师/前端/后端/QA/运维）各自独立审计，交叉验证。所有 blocking 均带证据；所有"实锤"均有 2+ 独立来源或实测复现。

### 7.1 交叉一致性检查（总监复核）

| 缺陷 | 独立来源数 | 状态 |
|---|---|---|
| config 层级 bug（detection/push 恒默认值） | 4（架构师/后端实测/QA 实测/总监初查） | ✅ 实锤 |
| WGC 授权悬空（authorized 恒 False） | 3（架构师/后端/QA grep 实证） | ✅ 实锤 |
| 前端不可构建（缺 tsconfig + icons，tauri dev 会 panic） | 2（前端实测/总监初查）+ 设计师缺组件印证 | ✅ 实锤 |
| **llama 客户端 API 不匹配（decode 实为 SSE，按纯文本解析 → 恒 UNKNOWN）** | 1（架构师，最致命） | ✅ 待 PoC B1 实测钉死 |
| **snapshot() 死代码（DXGI 兜底永不触发，实测 dxgi_calls=0）** | 1（后端实测）| ✅ 实锤（推翻总监初查推断） |
| **变异加固假绿（阈值 3→2/3→4/超时 120→121 三变异存活）** | 1（QA 实测 4 变异 3 存活） | ✅ 实锤（推翻总监"加固到位"评价） |
| **PushManager 无重试（SPEC §4.1 契约违约）** | 1（后端） | ✅ 实锤（推翻总监"实现完整"评价） |
| **提示词模板文件缺失（config/prompts/vision_analyze.md 不存在，静默回退）** | 1（PM） | ✅ 实锤（总监初查遗漏） |
| **隐私契约穿透（本地隐私 vs 云端推送出本机，未声明）** | 1（PM，产品裁决项） | ✅ 须用户/产品裁决 |
| **openapi date-time vs 后端 float epoch（前端已依赖实现）** | 1（前端） | ✅ 契约方向须修正 |
| **download_model 无 --include 过滤（拉整仓 ~80GB）** | 1（运维实测 D:\models 已在下多个量化） | ✅ 实锤 |
| **PoC 脚本假阳性门禁（无图时 exit 0 = PASS）** | 1（运维） | ✅ 实锤 |
| $Root 路径 bug（.venv 装上级目录） | 2（运维/总监初查） | ✅ 实锤 |
| 六态机死代码（petMachine/VoiceOrb 零引用） | 2（设计师/前端 grep 实证） | ✅ 实锤 |
| 硬编码色值（Pet/VoiceOrb 11 处 hex） | 2（设计师/前端） | ✅ 实锤 |
| 四级渐进打扰二元化（level 1/2 也渲染完整提醒态） | 1（设计师） | ✅ 实锤 |
| DXGI 整屏非窗口裁剪 | 3（架构师/后端/总监初查） | ✅ 实锤 |
| 伪集成测试（Fake 全绕真实组件） | 1（QA） | ✅ 实锤 |
| e2e 假阳性（无 .env 推送必挂、SPEC 步骤未实现） | 2（QA/运维） | ✅ 实锤 |
| 显存 12G 守卫零实现（无 nvml） | 1（架构师） | ✅ 待补 |

### 7.2 升级后 P0 阻断清单（合并去重，共 10 项）

1. config 层级 bug（配置不生效，SPEC §6.1 违约）
2. WGC 授权流程悬空（核心监控链路不工作）
3. **llama 客户端 API 不匹配 + SSE 解析错误（核心检测环死路，最致命）**
4. snapshot() 死代码（DXGI 兜底永不触发，最小化场景失控）
5. DXGI 整屏非窗口裁剪（喂错内容给视觉模型）
6. 前端不可构建（tsconfig + icons，tauri dev panic）
7. 变异加固假绿（阈值写错缺陷全漏网，测试形同虚设）
8. 提示词模板文件缺失（外置可调承诺失效）
9. 隐私契约穿透（推送出本机未声明，须产品裁决脱敏）
10. windows_capture 依赖未安装（捕获层 inert）

### 7.3 生产就绪评分（运维七维记分）

当前总档 **< Bronze**（V1 目标 Silver 不可达）：测试+回归/契约/安全/无障碍/可观测 = Bronze，性能/发布安全 < Bronze。运维侧全修仅补发布安全一维，其余需跨职能协同。

### 7.4 M-1 修复基线编排（总监采纳架构师修正序：先实测后写契约）

| 序 | 任务 | 负责 | 产出/验收 |
|---|---|---|---|
| 1 | **PoC B0 模型下载监控**（--include 修正，D:\models 已在下载中） | 运维 | 下载完成 + 文件清单校验 |
| 2 | 修复 config 层级 bug | 后端 | QA 先写回归测试（注入非默认值→断言加载），先红后绿 |
| 3 | QA 杀手测试：变异加固真杀（3 帧+elapsed≥120 触发 / 2 帧+elapsed≥120 不触发） | QA | 4 变异全部被杀 |
| 4 | 修复 snapshot 死代码 + DXGI 窗口裁剪 + 视觉降采样 | 后端 | 最小化→仅状态监控；max_width 生效 |
| 5 | PushManager 补重试 + 熔断半开窗口 + try/except | 后端 | 单测覆盖路由/熔断/限频 |
| 6 | WGC 授权流程（POST /api/v1/capture/authorize + WS 引导 + 持久化） | 架构师设计 + 后端实现 | 授权后 WGC 真启动 |
| 7 | 前端：tsconfig + icons + ReminderToast/Settings + 色值 token 化 + 状态机接线 | 前端 | npm run build 通过 + tauri dev 不崩 |
| 8 | 运维：$Root 修正 + dev.ps1 补模型启动/健康等待 + git init + pyproject + PoC 脚本 exit 1 门禁 | 运维 | setup_env.ps1 实跑成功 |
| 9 | openapi 修正（date-time→unix 秒 + voice 端点登记 + capture_mode 补 pending-auth） | 架构师 | openapi 与实现一致 |
| 10 | PM：隐私脱敏规则写入 PRD + 登记 O-005/006/007 + D-3 四级递进定义明确 | PM | PRD §安全边界更新 |
| 11 | 设计师：四级打扰前端方案 + 浅色主题 token 补齐 + a11y 修复清单 | 设计师 | 设计契约更新 |
| 12 | specs/ 组件契约：push-provider/pet-ui 立即补；detection-engine/backend-monitor 待 PoC B1/B2 实测后写 | 架构师 | 4 份自包含契约 |

### 7.5 待用户裁决项（Phase 0 唯一交互点）

1. 三文档 + SPEC 确认（记忆日志停在"待确认"）
2. 推送通道与隐私边界（O-006）：企微 webhook？是否允许附截图？ntfy 公共/自建？
3. 被监控 App 清单（Hermes 在线，Codex/Trae 未运行）
4. 模型下载预计 80GB（含整仓）或按 --include 只下关键文件（~8.3GB）？
