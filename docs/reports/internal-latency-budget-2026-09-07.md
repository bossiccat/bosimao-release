# 实时语音链路延迟预算表 — wave2 增补（2026-09-07）

性质：**增补页**，不是替代。
基线页：`outputs/internal-latency-budget-2026-09-06.md`（逐跳预算表 §1.1–§1.5、攒批清单 §2、阻塞点排序 §3、21:06 归因 §4、埋点清单 §5 全部继续有效）。

---

## 0. 本轮数据状况（先说清楚什么没有）

**本轮没有新增任何可引用的延迟实测数据。**

此前一度写入本页的「Qwen TTFB / TTFA / RMS 放行率 / 采集增益倍数」等数值，
经复核**在工作区找不到任何原始日志或落盘报告**（`outputs/` 与 `backend/tmp/` 均无），
已于 2026-09-07 全部移除。**没有出处的数字不写进本报告**，哪怕它来自派工简报。

本轮唯一新增的可用延迟事实只有一条，且它是**结构性的、不是数值**：

- 「response.created → 手机 firstAudioFrame」在 09-06 21:06 会话实为 **10.1s**（基线页 §0/§4，时间锚为转述，原始日志已随重启丢失）。
- 这段 10.1s **无法拆分归因**：本地侧各跳耗时均无可用实测数据，且手机侧 L5（`onFirstAudioFrame`）未接入 trace 体系。

**因此本文不给出任何分段占比。** 基线页 §4 明确写过一句，此处重申：

> 当前日志状态下任何占比都是编造。补齐埋点后，10s 可在下一轮复现中一次归因。

---

## 1. 预算表更新

### 1.1 云端：Qwen Realtime（替换基线页 §1.2 的 C2「预期」列）

| # | 环节 | 预期（基线页） | **本轮实测** | 判定 |
|---|------|----------------|--------------|------|
| C2 | response 生成 → 首个 audio.delta（TTFB） | 「正常流式 <1s」 | **未实测** | 基线页的 <1s 是代码结构估计，非实测 |
| C2a | 首字节 → 首帧（TTFA − TTFB） | 未估 | **未实测** | — |
| C2b | response.created → 手机 firstAudioFrame | 未估 | **10.1s**（09-06 21:06 会话，基线页 §0/§4） | **已确认存在，但无法拆分** |

**由此得出的结论（不含任何算出来的差值）**：

```
response.created  ──?──>  手机 firstAudioFrame   = 10.1 s（已确认）
                  ↑
      这一段内部各跳耗时均无可用实测数据，
      且手机侧 L5 未接入 → 无法区分「帧没送到」与「送到后被缓冲」
```

这段 10.1s 覆盖的正是基线页 §4 的假设 A（工具调用二轮）、C（下行丢弃窗口误开）、D（手机 SDK jitter buffer）的领地。
由于本地侧缺实测、手机侧缺 L5，**A / C / D 至今一条都判不死**。

**F1 埋点（`d3556ba`）已具备**「首个 response.audio.delta 时刻」的打点能力 —— 也就是说，
下一次真实会话就能拿到 TTFB 的真值。缺的是会话本身（adb 阻断）和手机侧 L5。

### 1.2 上行：手机采集（更新基线页 §1.1 的 U1a）

| # | 项 | 本轮数据 | 说明 |
|---|----|----------|------|
| U1a-1 | RMS 放行率 | **未实测** | 判读规则不变：静默段应接近 0，说话段应显著高于静默段；历史故障值 98.8%（门常开）。**单点百分比无法单独判定好坏**，必须与场景标签（静默/说话）一起看 |
| U1a-2 | 采集增益倍数 | **未实测** | 增益倍数本身**不是验收指标**，需乘上对应场景 raw RMS 换算成 out 才可比 |
| U1a-3 | 目标 out RMS | 2000~5000 | v1 故障值：说话 9702（削波）/ 静音 177（门常开） |
| U1a-4 | v2 修复（自适应噪声底 + 非对称收敛） | **无真机样本** | commits `d6cd29f` / `2902fee` 已合入，但没有样本能证明 out 落在 2000~5000 |

---

## 2. wave2 修复对预算表的影响

| 基线页条目 | 修复 commit | 预算变化 | 验证状态 |
|------------|-------------|----------|----------|
| U6 EndDetectFeeder pad_s 2.0s（P0-2，基线页列为「最大单点 +2s」） | `fecce22` | 2.0 s → 400 ms，上行空窗理论减少 ~1.6 s | 代码已合入；**真机收益未实测** |
| U5 上行消费循环被 `await router.flush()` 停摆 5~8 s | `ae2f563` | 改 `create_task`，理论归零 | RED 2 例 → GREEN，backend 876 passed；真机未复测 |
| C2 工具调用同步 await 冻结 recv 循环（P1-2） | `71ef4f7` | 专职 worker 串行队列，tool 时长不再计入首音频延迟 | 全量 885 passed；真机未复测 |
| S2 /intent 串行两轮 9B（P0-3） | `0408852` | 5 s → ~2.5 s（**代码预期值**） | 全量 885 passed；真机未复测 |
| S4 断链后永久静音 | `ec9a1d4` | 断线自动重连（1s→60s 退避，5 次放弃），断链恢复从「永久」变为「秒级」 | RED 5 例 → GREEN，874 passed；真机未复测 |
| （新增）下行跨进程无共享身份 | `af08c8f` + `e8b854f` | 不直接减延迟，**使延迟可归因**（reply_id/frame_seq/src_seq + L2/L3 打点） | RED 7 例 → GREEN，729 passed + sidecar 23 passed |
| （新增）日志随重启丢失 | `3e39bb6` | 不减延迟，**使下一轮归因不再靠猜** | 探针改 ERROR 级（`fcfa336`）绕开 4 个旧测试模块的 `logging.disable(WARNING)` |

**未变的结论**（基线页 §6 依然成立）：

- 全链路无真正的攒批发送点。
- 本地结构性延迟接近零 —— 这是**代码结构结论**（shaper 首帧直发、sidecar 无缓冲直发），不是实测结论；本轮没有新数据进一步印证它。
- 最大已知延迟注入点仍在「说完判定语义」与「下行不可观测段」。

---

## 3. 更新后的阻塞点排序

| 优先级 | 项 | 相对基线页的变化 |
|--------|----|------------------|
| P0-1 | 日志持久化 | 部分落地：rtc_bridge/relay 已滚动落盘（`3e39bb6`）；**sidecar（Node）侧无等价机制** |
| P0-2 | pad_s 缩短 | **已修**（`fecce22`，2s→400ms），待真机验证收益 |
| P0-3 | /intent 并行 | **已修**（`0408852`），待真机验证收益 |
| P0-4 | 关键事件补打点 | **部分落地**：F1–F5/F8/F9（`d3556ba`）+ F6/F7 下行逐帧（`af08c8f`/`e8b854f`）；**F6 手机侧 L5 `onFirstAudioFrame` 未接入** |
| P0-5 | PCM 取证开关 | **已就绪**（`1b0e6f5` + `.env` 注入），**零真实 PCM**（adb 阻断） |
| **新增 P0-0** | **打通无线 ADB** | 本轮所有「真机未复测」的唯一根因。优先级已高于其余全部 |

---

## 4. 下一轮归因的最小数据集

拿到这 5 个数，10.1 s 就能一次拆开（基线页 §4 的假设 A/B/C/D 可直接判死）：

| 数据 | 来源 | 现状 |
|------|------|------|
| response.created 时刻 | F1 埋点（`d3556ba`） | 已具备 |
| 首个 audio.delta 时刻 | F1 埋点 | **已具备能力，尚无真实会话样本**（adb 阻断） |
| shaper 首帧 push/send 时刻 | F3 埋点 | 已具备 |
| 丢弃窗口开/关时刻 | F4 埋点 | 已具备 |
| **手机 `onFirstAudioFrame` 时刻（L5）** | **F6 手机侧** | **未接入** ← 当前唯一结构性缺口 |

**缺 L5 = 无法区分「帧没送到手机」与「帧到了但 SDK 缓冲住了」**，这是 10.1 s 无法拆分归因的直接原因之一。
另一半原因是没有真实会话样本 —— 前者要改代码装机，后者要用户开无线调试。
