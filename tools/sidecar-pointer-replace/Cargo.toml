[package]
name = "sidecar-pointer-replace"
version = "0.1.0"
edition = "2021"
description = "ADR-027 Windows native atomic current.json pointer replacement helper"
license = "UNLICENSED"
publish = false

[dependencies]

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_Storage_FileSystem"] }
