# 方案 B｜2026-08-25 独立审计与发布门禁

## 裁决

**商业发布：NO-GO。**

本轮仅验证了代码、组件合同与本地控制面/桥接路径；尚未取得 Android 真机、真实 TRTC、真实音频、真实 APM/Brain、App 被杀恢复、消息丢失重试与性能基线的可审计证据。另有候选变更未形成可追溯提交 SHA，且成功冒烟的原始输出尚未落盘。

## 审计范围与方法

- 审计对象：`.worktrees/solution-b-control-plane`。
- 独立审查：QA 全量证据审计、后端 mTLS/冒烟审查、发布 QA 门禁审查。
- 本报告不把口头或会话内执行结果等同于可发布证据；除非有固定命令、候选 SHA 和成功日志文件，否则只作为“会话内复验信号”。

## 当前可确认的工程成果

1. hello proof 候选会话隔离：候选 B 仅在 hello 校验、redeem、候选启动成功后，通过 activation lock 接管 ACTIVE A；失败定向回复候选且释放资源。
2. 控制面终止账本与安全路由：terminate/status/retry/ack/wake 的契约和 guard 已接入当前 worktree。
3. bridge 控制面 reporter：bridge drained 与 APM cancelled 已以 fire-once/fail-safe 语义接线。
4. termination_id 代码中继：Android terminate → TRTC custom cmd → sidecar → bridge ctrl → control plane 的实现和单测路径存在。
5. Windows mTLS 客户端修复：redemption 与 ack client 使用显式 `SSLContext`、加载客户端证书并设置 `trust_env=False`。

## 独立审计证据

### 新鲜会话内复验（未落盘）

独立 QA 报告的会话内执行结果：

- 后端 unit/integration/contract：709 passed，2 warnings；
- 终止相关定向集：57 passed，1 warning；
- sidecar Node 测试：28/28 passed；
- termination-chain smoke：12/12 PASS，exit 0；
- `git diff --check`：exit 0。

**限制：以上结果没有重定向为可定位日志文件，不能单独构成发布包证据。**

### 变更与可追溯性风险

独立 QA 在 worktree 检出：31 个已跟踪修改文件、52 个未跟踪文件；tracked diff 为 2,135 additions / 561 deletions / 31 files，且当日无提交，HEAD 仍为 `ef22b89`。

结论：当前产物未形成候选 SHA，无法可靠归因、复现或发布。任何将 #21/#22/#23/#25/#26 标为“可发布完成”的做法都不成立；它们至多是“代码与本地自动化子任务完成”。

## mTLS 与冒烟专项审查

### 已确认

- `backend/rtc_bridge/redemption.py` 与 `backend/rtc_bridge/ack_reporter.py` 使用显式 CA 信任的 SSLContext、`load_cert_chain` 和 `trust_env=False`。
- MockTransport 分支使测试可运行，但当前单测未直接断言 SSLContext/AsyncClient 参数装配。

### 阻断与缺口

1. 现有 `outputs/smoke-termination-chain.log` 是历史失败日志（`cp_health_ready` ReadError、EXIT=1），不能作为 12/12 成功的交付证据。
2. 12/12 冒烟通过 WS ctrl 直接注入 `note_termination`，替代 Android → TRTC → sidecar 段；没有覆盖 android、APM、brain 或真实五 ack 聚合。
3. 临时 PKI 未验证正确的 Extended Key Usage（serverAuth/clientAuth）或错误 CA、错误客户端证书、主机名不匹配等负例；未使用真实部署证书和网关环境。
4. redemption 非 dict 响应的 fail-closed 异常类型需增强；ack reporter 的路径参数约束/编码需审查；应补 SSL 参数装配的专门契约测试。

结论：mTLS 代码方向可接受，**但生产安全证据不充分，不得作为真实部署 mTLS 已验收的依据。**

## G1–G8 发布门禁

| 门禁 | 裁决 | 可用证据 | 发布阻断原因 |
|---|---|---|---|
| G1 hello proof 隔离 | PARTIAL | 本地回归、受限独立 QA | 无 Android 真机 A/B 顶替证据 |
| G2 termination_id 全链 | PARTIAL | Python/Node/Android 单测、本地 WS 模拟 | 无 Android → 真实 TRTC cmd → sidecar 取证 |
| G3 双 ack | PARTIAL | 代码与单测 | 无真机/生产执行体的双报告及五 ack confirmed |
| G4 App 被杀恢复 | FAIL | 无 | 未执行 AC-2.1~2.3 |
| G5 错误流 | PARTIAL | hello 负例与本地模拟 | 无真机消息丢失 timeout/retry、A/B 顶替 |
| G6 本地全栈冒烟 | FAIL（按可审计证据） | 会话内 12/12 信号；落盘日志为失败 | 无可复现成功日志，且未覆盖设备/TRTC/APM/brain |
| G7 性能 | FAIL | 无 | 无 p95、tid 时延、双 ack 时延、50rps 数据 |
| G8 已知 P0 扫描 | PASS（仅静态口径） | 现有源码扫描 | 不抵消其它门禁失败；发布范围如含 `deploy/ws-test` 仍需处理 emoji 或排除发布范围 |

## 未验证的发布阻断项

- Android 真机 AC-1.1~1.5，特别是 custom cmd 在真实 TRTC 下送达 sidecar 的时延；
- App force-stop、sidecar 崩溃、pending claim 恢复和会话可重入；
- 丢失自定义消息后的 CP timeout/partial/retry，以及真机 A/B 顶替；
- 真实双工音频、真实 APM cancelled、Brain sealed 和五 ack 终态聚合；
- 性能：terminate p95 <500ms、tid <3s、双 ack <10s、50 req/s 无错误；
- 使用真实网关、正确 EKU 证书和负例的 mTLS 验收；
- 固定候选 SHA、完整版本控制清单与可复现日志包。

## 受控下一步（禁止边做边上线）

1. **版本冻结：**先确认 52 个未跟踪产物的发布归属，纳入版本控制并形成候选提交 SHA；不接受“仅工作树存在”的发布候选。
2. **证据固化：**在固定 SHA 上复跑后端、Node、Android、smoke；将命令、解释器版本、stdout/stderr、退出码和时间戳写入不可覆盖的候选证据目录。旧失败冒烟日志必须标明为历史失败，不得与成功证据混用。
3. **mTLS 补测：**由后端成员补 EKU、错误 CA、错误 client cert、hostname mismatch、SSL 参数装配及真实环境证书的验证。
4. **真机门禁：**按 `docs/plans/2026-08-25-realdevice-e2e-acceptance.md` 实测 AC-1~3、G4、G5、G7，采集 Android / TRTC / sidecar / bridge / control plane 同一时间线。
5. **二次独立 QA：**仅在固定 SHA、证据归档和真机门禁全部完成后，独立 QA 才能给出发布裁决。

## 最终结论

当前方案的结构（契约、mTLS、fail-closed、幂等、线性化、资源释放、ack 状态）具有商业化设计基础，不应将其称作一次性自用脚本；但交付过程尚不具备商业生产所需的版本可追溯性、真实环境实证和发布门禁闭环。

**因此：可以继续受控迭代，不得商业发布。**
